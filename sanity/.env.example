# Sanity Studio
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Only needed to run `npm run seed`. Create an Editor token at
# https://sanity.io/manage → your project → API → Tokens. Never commit it.
SANITY_WRITE_TOKEN=
